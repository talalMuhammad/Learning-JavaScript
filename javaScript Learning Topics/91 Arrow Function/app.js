function add(a, b) {
  return a + b;
}

// Arrow Function

let addArrow = () => {};

let sum = (a, b) => a + b;

console.log(add(3, 4));
console.log(sum(2, 4));

function double(a) {
  return 2 * a;
}

let doubleArrow = (a) => 2 * a;
